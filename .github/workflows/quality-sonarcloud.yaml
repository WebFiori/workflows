
name: Sonar Cloud Scan

on:
  workflow_call:
    inputs:
      coverage-file:
        required: true
        type: string
    secrets:
      SONAR_TOKEN:
        required: true

jobs:
  upload:
    name: Sonar Cloud Scan
    runs-on: ubuntu-latest
    steps:
      - name: SonarCloud Code Scan
        run: echo "Ok"
